# student_timetable_app

Student timetable app with Google sign-in restricted to `pwr.nu.edu.pk`.

## Getting Started

1. Install Flutter SDK.
2. In this directory, run:

```bash
flutter pub get
flutter run
```

## Note

You still need to configure Google OAuth credentials for Android/iOS/Web before authentication will work on devices.
